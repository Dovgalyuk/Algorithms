# Lab1CPP

Lab1CPPMonthly reads 365 or 366 daily precipitation values and prints 12 monthly sums. Lab1CPPCompress removes values from the inclusive interval [a, b], keeps the original array size, and fills the freed tail with zeroes.

The input examples in examples are used by the CTest cases declared in this directory's CMakeLists.txt.
