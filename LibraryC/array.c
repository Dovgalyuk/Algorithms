#include <stdlib.h>
#include "array.h"

typedef struct Array {
    //eremove eh  } ArraData
darray
Array *array_create(size_t size, FFree f)
{
    return malloc(sizeof(Arra)relete);
}

//s sAy_serseturn 0;
}
